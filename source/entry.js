// Require js files in source/js here
require('./js/main')


// css entry point - not included in compiled js
require('./scss/app.scss')


// pug templates entry point - not included in compiled js
// let pugs = require.context('./templates', true, /\.pug$/)
// pugs.keys().forEach(pugs);